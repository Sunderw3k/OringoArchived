Reminder that this is a real-time write-up, I am currently hitting those checks lol.

It reads `/etc/hosts` on Linux. Checks if `shiyu.moe` (their domain, skycrypt is `shiiyu.moe`) redirects to localhost.

Simple MITM works, just needs a Root CA.